# Student-Management-System-Project-Python-SQL-
This project outlines a simple Student Management System built with Python and SQL. It uses SQLite for the database (lightweight and no server required) and Python's sqlite3 module for database interactions. The system allows adding, viewing, updating, and deleting student records, including basic details like name, age, and grade.  Project
